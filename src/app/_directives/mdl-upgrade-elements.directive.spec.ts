import { MdlUpgradeElementsDirective } from './mdl-upgrade-elements.directive';

describe('MdlUpgradeElementsDirective', () => {
  it('should create an instance', () => {
    const directive = new MdlUpgradeElementsDirective();
    expect(directive).toBeTruthy();
  });
});
